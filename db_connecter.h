#pragma once
#include <string>

class DBConnecter
{
public:

	void Connect()
	{
		/// Connecting to data base
	}
	std::string SQLRequest(std::string request)
	{
		std::string answer;
		/// Sends the SQL query to data base
		/// Gets the answer
		return answer;
	}
};